# Vendored dmb_config

Pinned copy of DiamondMind `scripts/dmb_config/` for standalone lineup batting-order logic.

| Item | Value |
|------|-------|
| **Source** | `DiamondMind/scripts/dmb_config/` |
| **Sync** | `./scripts/sync-dmb-config.sh` from repo root |
| **Used by** | `lineup_config.py` → `order_lineup()` |
| **License** | Same as parent repo (MIT) |

Do not edit files here directly — sync from upstream and commit the vendor update.
